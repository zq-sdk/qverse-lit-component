---
description: 组件库与 samples 源码格式、命名与 TS 书写规范
globs: src/**,samples/src/**
alwaysApply: false
---

# 代码样式与规范

## 空行（重要 — 生成/修改代码时必须遵守）

> **Agent**：禁止「一行代码一行空行」。违反后文件体积会异常膨胀（例如 260 行变成 500+ 行），且与仓库现有风格不一致。常见失误见 `.cursor/rules/agent-pitfalls.mdc`。

**禁止「一行代码一行空行」**（不得在每个语句、import、字段之间插入空行）。

| 位置 | 规则 |
|------|------|
| 文件头 `*/` 之后 | **1** 行空行，再写 import |
| import 块与后续代码 | **1** 行空行 |
| 相邻 import | **不要**空行分隔 |
| 多行 `import { ... }` 内 | 每项之间**不要**空行 |
| 类/接口成员之间 | **1** 行空行（属性、方法块之间） |
| 方法体 `{` 与 `}` | `{` 后 **1** 行空行；`}` 前 **1** 行空行 |
| 方法体内连续语句 | **不要**空行（如 `this.a = x` 与下一行赋值） |
| 文件末尾 | 最多 **1** 行尾换行，**不要**连续多个空行 |

### 正确示例（`index.ts` 片段）

```ts
  setEnabled(value: boolean) {

    this.enabled = value
    this.toggleAttribute('enabled', value)

  }
```

### 错误示例（禁止 — Agent 曾反复犯此错）

```ts
  setEnabled(value: boolean) {


    this.enabled = value


    this.toggleAttribute('enabled', value)


  }
```

```ts
import { LitElement } from 'lit'

import { property } from 'lit/decorators.js'   // ❌ import 之间不要空行
```

**参考（正确风格）**：`src/components/common/lit-switch-dollhouse-view/index.ts`、`interaction.ts`；samples：`samples/src/components/common-demo/SceneExampleDemo.vue`。

**反例（勿模仿）**：曾被错误格式化的 `lit-switch-floor/index.ts`、`*Demo.vue`（若 `<template>` / `<script>` / `<style>` 内每行后都有空行，应按上表修正）。

## Vue 单文件组件（`.vue`）

**禁止在 `<template>`、`<script>`、`<style>` 内「一行代码一行空行」。**

| 区块 | 规则 |
|------|------|
| `<template>` | 标签、属性、子节点之间**不要**空行；多属性可换行但**行间无空行** |
| `<script setup>` | 与 `.ts` 相同：import 连续；类型/常量/函数之间**最多 1 行**分隔逻辑块，函数体内语句**连续** |
| `<style scoped>` | 选择器之间 **1** 行空行；规则块内属性**连续**，不要每条属性后空行 |
| 区块之间 | `</template>` 与 `<script>` 之间 **1** 行空行；`</script>` 与 `<style>` 之间 **1** 行空行 |

### 正确示例（`<script setup>` 片段）

```vue
<script setup lang="ts">
import { onMounted, ref } from 'vue'
import { SCENE_BOOT_DATA } from '@/config/scene-boot'

const floorSwitchOption = ref({ floors: [], currentFloor: 'all' as const })

watch(adaptedSceneData, (data) => {
  if (!data) return
  floorSwitchOption.value = {
    ...floorSwitchOption.value,
    floors: resolveFloorOptionFromAdaptedData(data),
  }
})
</script>
```

### 错误示例（禁止）

```vue
<lit-switch-floor

  :qspace="qspaceInstance"

  :option="floorSwitchOption"

/>

const floors = resolveFloorOptionFromAdaptedData(adaptedSceneData)

floorSwitchOption.value = { floors }
```

## 文件头

| 范围 | 文件头 |
|------|--------|
| `src/components/common/**/*.ts` | **必须** `@Author: ncz` 块（见 `common-components.mdc`） |
| `src/components/base/` | 可选；新文件建议与 common 一致 |
| `samples/src/**/*.ts` | 可选；复杂模块可加简短 `@Description` |

## 命名

| 类型 | 约定 | 示例 |
|------|------|------|
| 自定义元素 / 目录 | kebab-case | `lit-switch-dollhouse-view` |
| TS 类 | PascalCase | `LitSwitchDollhouseView` |
| 属性（Lit） | camelCase | `enabled` → attribute `enabled` |
| 常量 | UPPER_SNAKE 或 camelCase 导出 | `TAG`、`CLICK_EVENT` |
| 内部方法 | camelCase | `setEnabled` |
| 事件名 | kebab-case 字符串 | `lit-click` / `lit-switch-complete` |

## TypeScript / Lit

- 相对 import **带 `.js` 后缀**（ESM 构建）：`from './constants.js'`
- 组件注册统一 `@safeCustomElement(TAG)`，禁止裸 `customElements.define`
- 对外 API 用 `@property`；组件内部临时状态用 `@state()`，**不**作为对外 attribute
- 对象引用（如 `qspace`）用 `@property({ attribute: false })`
- 布尔 HTML attribute 复用 `properties.ts` 的 `booleanAttr` converter
- 复杂交互放 `interaction.ts`，`index.ts` 只做属性声明与 `render()` 委托
- 避免 `any` 扩散；qspace 等外部 SDK 可在边界处用 `any`，内部尽量收窄

## CSS（`styles.ts`）

- 使用 Lit `css`  tagged template；图标 `import url from './assets/icon.png?url'`
- Shadow 内样式；宿主覆盖用 CSS 变量（`--lit-mode-*-icon`）
- 选择器与作品编辑端 ModeSwitch 对齐（`.mode-item.<view>`）

## 注释

- 代码应自解释；**仅**对非 obvious 的业务/边界写 JSDoc（属性含义、attribute 映射、内部状态职责）
- 不写「翻译代码」的废话注释
- 禁止遗留 `console.log` 调试（samples 演示逻辑除外且应克制）

## 禁止

- 不要 reformat 整个文件为「每行后空一行」（含 `.vue` 的 template / script / style）
- 不要在 `common/` 根目录增加 shared 基类（各组件目录自包含）
- 不要为省事复制粘贴后留双倍空行或重复 import
- 不要用整文件 `Write` 替代小范围 `StrReplace` 除非用户明确要求重写该文件
